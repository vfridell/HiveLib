<Window x:Class="HiveDisplay.NewGameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ns="clr-namespace:HiveDisplay"
        Title="NewGameWindows" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25" />
            <RowDefinition Height="5" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center">Player 1</TextBlock>
        <TextBlock Grid.Row="0" Grid.Column="2" HorizontalAlignment="Center">Player 2</TextBlock>
        <ComboBox Grid.Row="2" Grid.Column="0" Height="25" ItemsSource="{Binding Source={x:Static ns:PlayerChoices.Players} }"></ComboBox>
        <!-- <RadioButton Name="Player1ChoiceRadio" Grid.Column="0" Grid.Row="2" DataContext="{Binding Source={x:Static HiveDisplay:PlayerChoices.Players}}"/> -->
    </Grid>
</Window>
